interface:
  display_name: "Delivery"
  short_description: "Plan and execute outcome-based delivery"
  default_prompt: "Use $delivery to show horizons, outcome plans, eligible features, blockers, WIP, and the next action."
